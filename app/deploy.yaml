apiVersion: apps/v1
kind: Deployment
metadata:
  name: poc-udd
  namespace: poc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poc-udd
  template:
    metadata:
      labels:
        app: poc-udd
    spec:
      containers:
      - name: poc-udd
        image: jhidalgoc/poc-udd:latest
---
apiVersion: v1
kind: Service
metadata:
  name: poc-udd
  namespace: poc
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 80
    name: poc-udd-port
  selector:
    app: poc-udd
---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: poc-ingress
  namespace: poc
spec:
  rules:
  - host: k8s-apps-previos.udd.net
    http:
      paths:
      -
        path: /mvcmovie
        pathType: Prefix
        backend:
          service:
            name: poc-udd
            port:
              number: 8000

